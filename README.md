# microsoft-entra-get-started
This repo contains a basic implementation of authentication via Microsoft Entra Id. Contains the .Net Secured Web Api, a Console client app connecting to the API and an Angular client app also autenticating via Microsoft Entra Id and calling the secured api
